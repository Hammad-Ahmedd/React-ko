import React from "react";

function Todobox({
    inputpara, del
}) {

    return (
        <div className="input-wrappers" >
            <p>{inputpara}</p>
            <button onClick={} className="btn" >Delete</button>
        </div>
    )
}

export default Todobox