import { createAction, createSlice } from "@reduxjs/toolkit"



export const NameSlice = createSlice({
    name: "Name",
    initialState: { [name,setname]: "Hammad" },
    reducers: {
        addname: (state, action) => {
          
        }
    }
})
